import React from 'react';

const EmptyContainer = () => {
  return (
    <div className="empty-container">
      <p className="text-muted">This container is empty.</p>
    </div>
  );
};

export default EmptyContainer;